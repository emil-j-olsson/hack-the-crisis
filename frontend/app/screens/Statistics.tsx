import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export const Statistics = ({}) => {
    return (
        <View><Text style={{color: '#fff'}}>Statistics</Text></View>
    );
}

const statistics_styles = StyleSheet.create({

});