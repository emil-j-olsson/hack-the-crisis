import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export const PreviousTrips = ({}) => {
    return (
        <View><Text style={{color: '#fff'}}>Previous Trips</Text></View>
    );
}

const previous_trips_styles = StyleSheet.create({

});