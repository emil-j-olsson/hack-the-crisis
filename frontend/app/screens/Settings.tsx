import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export const Settings = ({}) => {
    return (
        <View><Text style={{color: '#fff'}}>Settings</Text></View>
    );
}

const settings_styles = StyleSheet.create({

});